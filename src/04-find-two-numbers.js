/**
 * Given an array of integers nums and an integer target,
 * return indexes of the two numbers such that they add up to target.
 *
 * @param {Array} arr
 * @param {Number} target
 * @return {Array}
 *
 * @example
 * For arr = [2,7,11,15], target = 9 the result should be [0, 1],
 * because 2 + 7 = 9.
 *
 */
function findTwoNumbers(/* arr, target */) {
  throw new Error('Not implemented');
}

module.exports = findTwoNumbers;
