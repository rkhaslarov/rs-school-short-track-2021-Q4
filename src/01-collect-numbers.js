/**
 * Implement the function collectNumbers which gets sorted array as an argument
 * merges it with internal one and returns the function for the next iteration.
 *
 * Important! You'll never know how many time the returned function will be called,
 * so make it generic.
 *
 * Additional points for O(n) solution.
 *
 * @param {Array} arr
 * @return {Function}
 *
 * @example
 * collectNumbers([1, 2, 3, 4])([1, 5])()
 *
 * The result should be [1,1,2,3,4,5]
 */
function collectNumbers(/* arr */) {
  throw new Error('Not implemented');
}

module.exports = collectNumbers;
