/**
 * Given a list of email addresses, return  the number of correct ones.
 *
 * For email prefix (before @):
 * Allowed characters: letters (a-z), numbers, underscores, periods, and dashes.
 * An underscore, period, or dash must be followed by one or more letter or number.
 *
 * For email domain (after @):
 * Allowed characters: letters, numbers, dashes.
 * The last portion of the domain must be at least two characters, for example: .com, .org, .cc
 *
 * @param {Array} emails
 * @return {Number}
 *
 * @example
 * For the input ['a@gmail.com', 'b@gmail.com', 'c@gmail.com'], the output should be 3
 *
 */
function getCorrectEmails(/* emails */) {
  throw new Error('Not implemented');
}

module.exports = getCorrectEmails;
