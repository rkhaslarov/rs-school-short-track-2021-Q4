/**
 * Your task is to implement a function,
 * that returns N smallest elements of input array in the original order
 * Also:
 *  - the number of elements to be returned cannot be higher than the array length;
 *  - elements can be duplicated;
 *  (in case of duplicates, just return them according to the original order)
 *
 * @param {Number[]} array
 * @return {Number}
 *
 * @example
 * findSmallestNumbers([1, 2, 3, 1, 2], 3) -> [1, 2, 1]
 *
 */
function findSmallestNumbers(/* array, number */) {
  throw new Error('Not implemented');
}

module.exports = findSmallestNumbers;
