/**
 * Given an array, you should rotate by k steps to the right.
 * Note: It should be implemented as in-place algorithm.
 *
 * @param {Array} arr
 * @param {Number} k
 * @return {Array}
 *
 * @example
 * For ([1,1,2,3,3], 2) should be [3,3,1,1,2]
 * For ([1,2,3,4,5], 1) should be [5,1,2,3,4]
 */
function rotateList(/* arr, k */) {
  throw new Error('Not implemented');
}

module.exports = rotateList;
